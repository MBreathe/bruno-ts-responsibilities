info:
  name: Create a task
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/tasks
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
