info:
  name: Delete a task
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:3000/api/tasks/:id
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
