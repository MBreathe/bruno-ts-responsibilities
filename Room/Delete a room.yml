info:
  name: Delete a room
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:3000/api/rooms/:id
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
