info:
  name: Get a room
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/api/rooms/:id
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
