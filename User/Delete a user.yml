info:
  name: Delete a user
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:3000/api/users/:id
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
