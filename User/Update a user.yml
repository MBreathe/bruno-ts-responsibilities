info:
  name: Update a user
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:3000/api/users/:id
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
